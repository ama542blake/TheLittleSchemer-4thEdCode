#lang scheme

(module Little_Schemer
  (provide '(atom? lat?)))

(define atom?
  (lambda (x)
    (and (not (pair? x)) (not (null? x)))))

(define lat?
  (lambda (l)
    (cond
    ((null? l) #t)
    ((atom? (car cdr)) (lat? (cdr l)))
    (else #f))))

(export '(lat? atom?))

